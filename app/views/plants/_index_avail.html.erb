<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @plants.each do |plant| %>
      <tr id="<%= dom_id(plant) %>">
        <td><%= plant.plant_name %> | </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
