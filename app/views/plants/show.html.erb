<% if user_signed_in? %>
  <%= render 'details' %>
  <div class='row'>
    <%= link_to 'Edit', edit_plant_path(@plant) %> |
    <%= link_to 'Delete', plant_path, method: :delete, data: { confirm: 'Are you sure?' } %> |
    <%= link_to 'All Plants', plants_path %>
  </div>
<% else %>
  <%= render 'unauthorized.html.erb' %>
<% end %>
